import { defaultArticleState } from './constants/articleProps';

export const settingsForm = Object.assign({}, defaultArticleState);
export const settingsPage = Object.assign({}, defaultArticleState);
